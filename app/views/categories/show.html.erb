<h2><%= @category.name %></h2>
<% if @category.games.any? %>
	<ul class = "gamelist">	
	<% @games.each do |game| %>
		<%= render :partial => "categories/gamelist", :locals => {:game => game} %>
	<% end %>
	</ul>
	<div class="clear"></div>
	<%= will_paginate @games%>
<% end %>