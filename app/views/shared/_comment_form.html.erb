<%= form_for(@comment) do |f|%>
	<div class="field">
		<%= f.text_area :content, placeholder: "...", size: "24x10" %>
	</div>
	
	<%= f.hidden_field :game_id, { :value => @game.id } %>
	
	<%= f.submit "Post", class: "btn btn-large btn-primary" %>
<% end %>